[package]
name = "sbin"
description = "🧱 Fetches and updates binaries from Docker images—simple, fast, Rust-powered."
version = "0.1.2"
edition = "2021"
license = "MIT"

[dependencies]
clap = { version = "4.5.20", features = ["derive"] }
reqwest = { version = "0.11", default-features = false, features = ["blocking", "json", "rustls-tls"] }
serde = { version = "1.0", features = ["derive"] }
tar = "0.4"
flate2 = "1.0"
anyhow = "1.0"